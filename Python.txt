------------------------------------------------------------------------------------------------------------------
Sitio
------------------------------------------------------------------------------------------------------------------
Cursos sites.google.com/view/growinguptogether
------------------------------------------------------------------------------------------------------------------
-- >> Calculadora en 2 lineas
------------------------------------------------------------------------------------------------------------------
x = input('Wirte a math problem: ')
input('inout answer is : ', eval(x))
------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------
-- >> Importar Excel
------------------------------------------------------------------------------------------------------------------
pip install pandas
pip install openpyxl

import pandas as pd

data = pd.read_excel(' ... Aqui va la ubicación del Archivo ...')

print(data)
------------------------------------------------------------------------------------------------------------------
-- >> Terminos:
------------------------------------------------------------------------------------------------------------------
Indentación -->> Espacio entre la linea de codigo (Bloque de Codigo)
Comentar -- >> Uitlizamos # pero para multilinea ''' ... '''
Declarar Variables -- >> (x, y, z = "Hugo", "Paco", "Luis" -- >> Tres variables diferentes con tres valores diferentes) o (x = y = z = "Patos" -- >> Tres variables con un mismo valor)
Type( .. variable .. ) -- >> Identifica q tipo es INT or STR ...
"{}".format(x) -- >> Convierte la variable numero en str para poder sumarla con una variable cadena (x=5) "{}".format(x) + "Hola" o tambien se puede -- >> x,"hola"
\ -- >> Todo lo q venga despues de este caracter de escape no lo tomara como parte del codigo " hola \"mundo\" " = hola "mundo"
------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------



git clone "URL"
git init (Inicializa pero debe estar dentro de la carpeta dond quiere trabajar)
git add .
git commit -m "..."
git branch (Ramas)
git push /// git push origin "Nom Rama"
git branch "Nomb. New Rama"
git checkout "Nomb. Rama" (Pasar a otra rama (primero debo gaurdar cambios o no deja cambiar a la otra rama))
git diff (Ver cambios o diferencias entre los archivos locales y en la nube)
git fetch (Ver cambios en el repositario)
git merge "Nomb. Rama" (Combina todo)

